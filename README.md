# Simple Interest Calculator

A simple web-based application built using HTML and JavaScript to calculate Simple Interest.

## Formula Used
The formula to calculate simple interest is:
SI = (P × R × T) / 100

Where:
- P = Principal Amount
- R = Rate of Interest per annum
- T = Time period in years